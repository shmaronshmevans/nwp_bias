# You need to specify the algorithm and hyperparameters to use:

{
    # Pick the Bayes algorithm:
    "algorithm": "grid",

    # Declare your hyperparameters:
    "parameters": {
        "num_layers": {"type": "integer", "min": 3, "max": 10},
        "learning_rate": {"type": "float", "min":9e-6, "max":1e-3},
        "forecast_lead": {"type": "integer", "min": 12, "max": 250},
        "weight_decay": {"type": "float", "min": 0, "max": 1},
    },

    # Declare what to optimize, and how:
    "spec": {
        "metric": "loss",
        "objective": "minimize",
    },
    "trials": 30
}
